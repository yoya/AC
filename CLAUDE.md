# AC (AccountCluster)

FFXI の Windower アドオン。複数アカウントの操作を減らすための自動化。
詳細は [README.md](README.md)、構成の決めごとは [ARCHITECTURE.md](ARCHITECTURE.md)。

## 開発環境

- **Lua 5.1**（Windower が改造したもの）。5.2 以降の書き方は使わない。特に:
  - `pcall` の内側で `coroutine.sleep`（yield）できない。tick から呼ぶ処理は pcall で囲まない。
  - `math.random` は seed しないと毎回同じ列。ゆらぎを使う処理は注意。
  - for ループの制御変数は代入可（5.4+ の const 化とは違う）。
- **静的チェック**: `~/.luarocks/bin/luacheck .`（Lua 5.4 版）。設定は `.luacheckrc`。
  `/opt/homebrew` の luacheck は 5.5 で壊れているので使わない。
- **パケット定義**: `~/git/Windower/Lua/addons/libs/packets`。生バイトを直書きせず
  `packets.new` / `packets.inject` を使う。
- `zone/<数字>_*.lua`（約170個）は座標などのデータ定義。一括で触らない。
- 日本語クライアント前提。アイテム名・魔法名はコマンド文字列に直書き。

## コーディング

- 頼まれた範囲を、頼まれたスコープでやりきる。良い案があれば一言添えて、頼まれた通り進める。
- 1回きりの処理にヘルパーを作らない。
- 開いていないファイルについて推測しない。参照されたファイルは必ず読んでから答える。

## サブエージェント

- 数手で終わる仕事は振らない。自分の検証のために分身を使わない。1体で済むなら1体。

## コミット

- 目的ごとに分ける。ひとつのコミットを巨大にしない。

## 返答

- 短く、要点だけ。前置きや但し書きは最小限にして、答えそのものに文字数を使う。
- 「説明して」と頼まれたら、詳しく知りたいと言わない限り要点のまとめだけ返す。

## 進め方

- 作業を始める前に、これから何をするかを一文だけ言う。
- 作業中の報告は、重要なことが分かったときと方針を変えるときだけ。
- 終わったら結論から。最初の一文で「何をしたか」「何が分かったか」に答え、細かい話は後。
- 敵対的な観点で設計を保守的に進めてください。特に Opus 5 モデルは気をつけて下さい。

## 文書を書くとき

- 必要な長さに収める。中身は省かなくてよいが、埋めるための章・同じ内容の繰り返し・
  お決まりの前置きで長くしない。

## 訂正

- 前に言ったことの訂正は、その間違いで僕のコードや判断が変わるときだけ。短く言ってそのまま続ける。
- 何も変わらない些細な言い間違いは、黙って直して進める。
